---
# export ANSIBLE_STDOUT_CALLBACK=debug
- name: Dump generated ACL for a given OS.
  hosts: localhost
  gather_facts: no
  
  tasks:
  - name: Run this module to generate a Juniper SRX filter
    nleiva.capirca_acl.translate:
      platform: 'srx'
      filter_options:
        - Untrust
        - DMZ
      comment: "from zone Untrust to zone DMZ"
      def_folder: "integration/targets/translate/files/def"
      pol_file: "integration/targets/translate/files/policies/srx.pol"
    register: testout
  
  - name: Dump the resulting ACL
    debug:
      msg: "{{ testout.message }}"